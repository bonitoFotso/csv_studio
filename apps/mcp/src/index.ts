// Serveur MCP (stdio, local uniquement) — squelette du workspace, implémentation prévue
// à la phase suivante (outils profile_csv/preview_pipeline/apply_pipeline/match_files/
// find_duplicates/build_report). Volontairement non implémenté ici : cette phase ne fait que
// mettre en place la structure du monorepo.

export {};
